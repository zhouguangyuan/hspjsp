package com.hsp.service;

import com.hsp.domain.Users;

public class UsersService {
	//验证用户是否合法
	public boolean checkUser(Users user){
		//到数据库去验证
		String sql="seclect * from users where id=? and pwd=?";
		String paras[] = {Integer.parse(user.getId()), user.getPassword()};
		return true;
	}

}
